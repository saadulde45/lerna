import React from 'react';

class ReactApp extends React.Component {
    render() {
        return (
            <div>
                React Child
        </div>
        );
    }
}

export default ReactApp;