import React from "react";
import ReactApp from "react-app";

class App extends React.Component {
    render() {
        return (
            <div>
                Test parent
            <ReactApp></ReactApp>
            </div>
        );
    }
};

export default App;
